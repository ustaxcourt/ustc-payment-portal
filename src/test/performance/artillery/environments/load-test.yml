config:
  target: "http://localhost:8080"

  http:
    timeout: 15000
    pool: 200

  ensure:
    maxErrorRate: 10

  maxVusers: 500

  phases:
    - duration: 120
      arrivalRate: 10
      name: heavy-load-10-rps

  processor: "../processor.js"

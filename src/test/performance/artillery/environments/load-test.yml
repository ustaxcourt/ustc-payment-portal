config:
  target: "http://localhost:8080"

  http:
    timeout: 26.5
    pool: 200

  ensure:
    maxErrorRate: 0

  maxVusers: 500

  phases:
    - duration: 300
      arrivalRate: 7
      name: "demonstrated-safe-42-portal-rps-with-2-workers"

  processor: "../processor.js"

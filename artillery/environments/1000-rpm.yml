config:
  target: "http://localhost:8080"

  http:
    timeout: 60000
    pool: 50

  plugins:
    fake-data: {}

  ensure:
    maxErrorRate: 100

  phases:
    - duration: 30
      arrivalRate: 2
      name: debug-low-load

  processor: "../processor.js"

config:
  target: "http://localhost:8080"

  http:
    timeout: 60000
    pool: 50

  plugins:
    fake-data: {}

  ensure:
    maxErrorRate: 100

  phases:
    - duration: 30
      arrivalRate: 167
      name: stress-10000-rpm

  processor: "../processor.js"

config:
  target: "http://localhost:8080"

  http:
    timeout: 60000
    pool: 1000

  ensure:
    maxErrorRate: 100

  phases:
    - duration: 300
      arrivalRate: 167
      maxVusers: 10000
      name: stress-10000-rpm

  processor: "../processor.js"

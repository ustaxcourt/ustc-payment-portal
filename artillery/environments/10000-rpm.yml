config:
  target: "http://localhost:8080"

  http:
    timeout: 60000
    pool: 200

  ensure:
    maxErrorRate: 10

  phases:
    - duration: 300
      arrivalRate: 167
      name: stress-10000-rpm

  processor: "../processor.js"

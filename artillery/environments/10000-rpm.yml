config:
  target: "http://localhost:8080"

  http:
    timeout: 60000
    pool: 500

  ensure:
    maxErrorRate: 100

  maxVusers: 2000

  phases:
    - duration: 180
      arrivalRate: 167
      name: stress-10K-rpm

  processor: "../processor.js"
